# 2026-01-28 작업 요약(아주 쉬운 말로도 함께)

## 오늘 한 일(What)
- 대시보드 타임라인 카드를 "오늘 할 일" 단일 뷰로 전환하고, 전/다음 날 이동 기능 추가
- 선택한 날짜를 `요일 · yyyy.MM.dd` 형식으로 표시
- 모바일에서 텍스트가 끊기거나 밀리지 않도록 레이아웃 안정화(`min-w-0`, `truncate`, `shrink-0` 적용)
- 헤더의 "Second Semester"를 홈으로 이동하는 링크로 변경
- 라이트/다크 테마가 제대로 바뀌도록 CSS 변수 분리(:root=라이트, .dark=다크)
- 개발 로드맵 문서 작성(docs/정리/plans/roadmap.md)

## 왜 했나요(Why)
- 주간 격자보다 오늘 해야 할 일을 바로 보여주는 게 빠르고 명확해서 UX 향상
- 날짜/요일을 명시해 혼동을 줄이고, 좌우 이동으로 일정 탐색을 쉽게
- 모바일 화면에서 글자가 줄바꿈으로 깨지는 문제를 줄여 가독성 개선
- 브랜드 로고/텍스트를 홈 링크로 두어 어디서든 쉽게 대시보드로 복귀
- 테마 토글 시 색이 올바르게 바뀌도록(라이트/다크 변수 분리) 신뢰도 향상
- 앞으로의 개발 방향(상태 관리/상세 페이지/차트/a11y)을 한눈에 보이도록 로드맵 준비

## 어떻게 했나요(How)
- WeeklyTimelineCard.tsx
  - 오늘 기준 요일 선택, 좌우 버튼으로 요일 순환
  - 가운데에 `요일 · yyyy.MM.dd` 표시
  - 텍스트 레이아웃 안정화를 위한 유틸리티 클래스 적용
- app/layout.tsx
  - 헤더의 "Second Semester"를 `Link`로 변경, 홈(`/`)으로 이동
- app/globals.css
  - :root(라이트), .dark(다크) 팔레트 분리
- 문서
  - docs/정리/plans/roadmap.md 로드맵 작성(Phase/체크리스트)

## 아주 쉬운 말로(5살도 이해하기)
- 오늘 해야 하는 일을 한 장에 딱 보여줘요.
- 왼쪽/오른쪽 버튼을 누르면 어제, 내일도 볼 수 있어요.
- 오늘이 몇 월 며칠인지 글자로 같이 보여줘요.
- 작은 폰에서도 글자가 안 찌그러지게 했어요.
- 위에 있는 "Second Semester" 글자를 누르면 처음 화면으로 돌아가요.
- 밝은 모드/어두운 모드를 바꾸면 색도 알맞게 바뀌어요.

## 다음에 할 일(Next)
- Zustand 스토어 만들기(할 일/출석/타임라인/진행률)
- 페이지를 목데이터 → 스토어 연결로 전환
- /timeline 상세: 주/월 전환, 태그 필터, 완료/편집
- /progress 차트: 축/툴팁/범례 추가
- 접근성/반응형 디테일(포커스, aria, 대비)

## 참고(파일)
- app/_components/WeeklyTimelineCard.tsx
- app/layout.tsx
- app/globals.css
- docs/정리/plans/roadmap.md

---

## 오후 작업 추가 (학습 생성/관리 흐름, 헤더 전환)

### 오늘 한 일(What)
- 메인: 히어로 섹션 + 학습 생성 모달 + 최근 학습 간단 목록 추가
- 마이페이지(/mypage): 학습 목록 상세 관리(삭제/참여 토글/진척도 슬라이더) 구현
- 전역 헤더: 현재 경로에 따라 버튼 라벨이 자동 전환되는 MY ↔ MAIN 스위치 도입
- 학습 상태 저장소 추가: lib/store/learnings.ts (Zustand)

### 왜 했나요(Why)
- 메인 화면의 목적을 “학습 생성”으로 선명화하고, 생성 결과를 곧바로 확인할 수 있도록 함
- 관리(삭제/참여/진척도)는 MY(마이페이지)로 분리해 화면의 역할을 명확히 유지
- 상단 버튼이 현재 위치에 맞게 MY/MAIN으로 바뀌어 탐색 혼란을 줄임

### 어떻게 했나요(How)
- app/page.tsx
  - 히어로(설명 + “학습 만들기” 버튼)
  - 인라인 생성 모달(Form)로 학습 생성 → 목록 즉시 반영
  - 최근 학습 간단 목록(생성일, 진척도 조절, 참여 토글, 삭제)
- app/mypage/page.tsx
  - 동일한 정보/액션을 더 넉넉한 공간에서 관리
- lib/store/learnings.ts
  - `learnings` 상태, `addLearning/removeLearning/toggleJoined/updateProgress` 액션
- components/HeaderNavSwitch.tsx
  - 현재 경로가 `/`이면 라벨=MY, 링크=/mypage
  - 현재 경로가 `/mypage`면 라벨=MAIN, 링크=/
- app/layout.tsx
  - 기존 고정 링크 대신 [HeaderNavSwitch](cci:1://file:///Users/gimbyeong-gwan/CascadeProjects/second-semester/components/HeaderNavSwitch.tsx:4:0-14:1) 사용

### 아주 쉬운 말로(5살도 이해하기)
- 메인에서 큰 버튼을 눌러 공부 계획(학습)을 만들어요. 만들면 아래 목록에 바로 보여요.
- 위에 있는 버튼은 내가 있는 곳에 따라 “MY” 또는 “MAIN”으로 바뀌어요. 눌러서 쉽게 오갈 수 있어요.

### 다음에 할 일(Next)
- 학습 생성 폼 확장: 설명/태그/기간 추가
- 목록 검색/정렬(생성일/진척도), 참여만 보기 필터
- 빈 상태에서 더 친절한 가이드 카피/툴팁 강화
- 접근성(a11y) 보강: 폼 라벨/aria, 포커스 트랩(모달), 키보드 조작

### 참고(파일)
- app/page.tsx
- app/mypage/page.tsx
- app/layout.tsx
- components/HeaderNavSwitch.tsx
- lib/store/learnings.ts